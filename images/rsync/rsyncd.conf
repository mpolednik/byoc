gid = 0
uid = 0
log file = /dev/stdout
reverse lookup = no
[src]
    hosts allow = *
    read only = false
    path = /vol/src
    comment = input sources
[out]
    hosts allow = *
    read only = false
    path = /vol/out
    comment = build output
