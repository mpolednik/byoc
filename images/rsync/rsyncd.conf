gid = 0
uid = 0
log file = /dev/stdout
reverse lookup = no
[build]
    hosts allow = *
    read only = false
    path = /build/src
    comment = input sources
[out]
    hosts allow = *
    read only = false
    path = /build/out
    comment = build output
